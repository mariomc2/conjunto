<h1>Editing Resident</h1>

<%= render 'form', resident: @resident %>

<%= link_to 'Show', apartment_resident_path(@apartment, @resident) %> |
<%= link_to 'Back', apartment_residents_path(@apartment) %>
